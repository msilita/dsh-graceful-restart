# dsh-graceful-restart v0.4.12 — 禁止超时判断，故障判定全部事件驱动

## 问题背景（用户反馈）

v0.4.11 的故障页检测用**轮询 DOM + 时间窗口**（2s 首检 + 3s 轮询），
把正常页误判为故障页（页面内容只要含 "Failed to load plugins" 文本就会误报，
比如会话历史里的这段文字）。用户要求：**禁止任何超时判断**。

## 本次改动

### client 半（lib/client.js）：故障页检测改为事件驱动

- 移除 DOM 轮询（setTimeout/setInterval）——不再看页面文本。
- 改为监听 **`internal/status`** 事件（DSH loader fiber 状态广播，dsh-client-web
  内核用同一事件驱动故障页渲染）：任何 entry 进入 **`failed` 终态** → boot
  必然停留故障页 → 立即上报 host /page-failed。
- 只认 `failed`（与内核 AppRoot 判定 `s === "failed"` 一致）——loading/pending
  等中间态不报，正常加载永不误判。
- 无轮询、无时间窗口、无超时。

### host 半（lib/index.js）：所有故障判定移除超时

| 位置 | 改动 |
|---|---|
| runConnectionCheck | 移除 3 次 × 5s 重试上限——持续探测直到连接就绪（无超时）；**不主动报 refresh-failed**（失败判定交给确定性信号：boot 输出扫描 + client 半故障页上报） |
| tryResumeSessions | 移除 60s 超时——持续轮询直到所有目标会话恢复并 steer（无上限） |
| scheduleClientProbe | 移除 3 次重试上限——持续探测直到全部 bundle 可达（无超时） |
| 唤醒闸门 | 移除 15s 兜底——纯事件驱动（等 probe-ok），无超时 |

### 保留的"时间"仅用于非判定用途

- 70s stale 重置：清理卡住的安排（不涉及正确性判断）。
- 等待进度打印（5s 间隔）：仅终端反馈。
- 探测间隔（2s/5s）：重试节奏，不是判定条件。

## 验证要点

- 正常 3 包：internal/status 无 failed → 不上报 → 正常。
- 装 dsh-qq-client → 重启 → 浏览器端 qq-client entry failed → client 半
  监听 internal/status 收到 failed → 上报 → host 回滚卸载 → 重试成功。
- 慢启动正常系统：连接持续探测直到就绪，无超时误判。
