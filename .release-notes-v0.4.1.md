# dsh-graceful-restart v0.4.1 — 关闭/重启终端可见反馈 + 关闭强制上限

## 问题背景

v0.4.0 之后，用户安排关闭（shutdown_harness）时终端**没有任何输出**：

- `scheduleExit` 只写日志文件，不打印终端；
- 等待期间 `checkIdleAndExit` 每秒只写日志，用户看不到"在等谁"；
- 另一个被唤醒的会话（空会话恢复后一直 running）无限阻塞关闭——等待只有
  70s 后**静默重置**（用户更看不到任何反馈），用户以为关闭没生效，只能 Ctrl+C。

## 本次改动（lib/index.js）

### 1. 安排时立即打印（用户马上看到反馈）

`scheduleExit` 打印：

```
[dsh-graceful-restart] 关闭已安排：等当前轮次结束后进程退出（不重启）。等待期间终端将显示进度。
[dsh-graceful-restart] 重启+唤醒已安排：等当前轮次结束后进程退出并拉起新一代。...
```

### 2. 等待期间每 5s 打印进度（知道在等谁）

`checkIdleAndExit` 打印：

```
[dsh-graceful-restart] 关闭等待中：还有 1 个会话在运行（80c3d7f3），轮次结束后自动执行...
```

### 3. 空闲/超时都有终端输出

- 所有会话空闲 → `所有会话已空闲，执行关闭...`
- 70s 兜底重置 → `等待超时（70s）：仍有会话在运行，关闭安排已重置。如需继续，请再次触发。`
- 取消 → `关闭安排已取消，进程将继续运行。`

### 4. 关闭强制上限（45s）

用户显式**关闭**的意图优先于未完成的轮次：等待超过 45s 仍有 running agent
（典型：空会话被唤醒后一直 running 的"幽灵 agent"）→ 打印警告并强制执行退出。
**重启保持原语义**（等轮次结束，70s 兜底重置），避免打断正在收尾的工作。

## 文件

- `lib/index.js`：`scheduleExit` / `cancelPendingExit` / `checkIdleAndExit` 终端反馈。
