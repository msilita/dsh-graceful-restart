# dsh-graceful-restart v0.4.13 — 故障页检测：拦截真实 loader 错误输出

## 问题背景（用户反馈 + 复测发现）

1. v0.4.12 的 `internal/status` 事件**对插件不可见**——复测（装 dsh-qq-client 重启）
   无 page-failed 上报、无回滚：系统正常启动（host 半 OK），页面实际是故障页。
2. 用户指出关键突破口：**浏览器控制台能看到 loader 错误**（`exports is not defined`
   / `did not activate`）——这是错误页的**确定性信号**。
3. 用户提出"注入"思路：在错误页/正常页都注入检测代码，即可知道进入了哪个界面。
4. **重要 UX 问题**：故障页时连提问 UI 都不渲染（boot 未 settled，无 partial UI）——
   用户看不到任何交互，必须让系统快速识别并回滚脱离故障页。

## 方案：client 半拦截真实错误输出（事件驱动，无轮询/无超时）

client 半在 apply 时（错误页场景下 entry 未失败，apply 仍执行）注册三路检测：

1. **拦截 console.error**：DSH 内核 loader 失败时 `console.error` 输出
   （`web boot: N entries did not activate` / `failed to import loader entry ...`）——
   包装 console.error，匹配 loader 错误特征 → 上报。
2. **window error / unhandledrejection**：捕获未捕获异常与动态 import 失败。
3. **MutationObserver**：观察 DOM，AppRoot 渲染失败标题 "Failed to load plugins"
   （内核硬编码、叶子节点精确文本）→ 上报。结构性检测，不匹配页面内容。

特征模式（`isLoaderError`）：`did not activate` / `loader entry` /
`Failed to load plugins` / `exports is not defined` / `Cannot use 'import'` /
`failed to import`——只匹配真实错误输出，正常页不误报（已验证）。

上报 → host `/page-failed` → refresh-failed 统一故障链（回滚坏插件 → 重试）。

## 验证要点

- 装 dsh-qq-client → 重启 → 浏览器加载其 client 失败 → console.error /
  MutationObserver 捕获 → 上报 → 回滚卸载 → 重试成功 → 页面正常。
- 正常 3 包：无 loader 错误输出 → 不误报。
