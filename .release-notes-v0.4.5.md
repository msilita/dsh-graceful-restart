# dsh-graceful-restart v0.4.5 — 回滚完全不修改快照链

## 问题背景（用户反馈）

回滚重试时 `spawnChild` 走的是"正常启动扫描"逻辑：实际清单 vs current 指向
快照差集非空 → **删除比 current 新的条目 + 建新快照 + current 前移**。这在
回滚场景下是错的：

1. **回滚语义被破坏**：回滚 = 回到某个成功基线并验证它。重试成功时 current
   应停留在该基线，而不是"又建一个新快照并前移"——快照链被回滚重试污染
   （每次失败重试都留下一条新快照）。
2. **循环加剧**：逆操作按历史快照旧包名装回（如 `@my/wake-trigger`，实际
   注册为 `dsh-waker-trigger`）时，实际清单与基线快照永远不一致 → 重试时
   差集非空 → 又建新快照 → 又失败 → 又回滚……（配合误判触发器即无限抖动）。

用户指出：**应该先移动 current（已有），并且回滚时不能修改快照链**——
包括"清理比 current 新的条目"也是在改链。包名漂移没关系——链代表意图状态，
不要求与实际清单逐键一致，由正常启动（用户主动变更）时再对齐。

## 本次改动（lib/index.js）

### spawnChild(wake, rollbackRetry)

- 新增 `rollbackRetry` 参数（默认 false）。
- `rollbackRetry=true`（失败回滚后的重试）：
  - **完全不修改快照链**：不新建、不删除（连"清理比 current 新的条目"也去掉）、
    不移动 current——回滚语义 = 回到基线验证；
  - `lastAdded = []`（逆操作已卸载新增插件，不重复 client probe）；
  - 包名漂移（如 `@my/wake-trigger` ↔ `dsh-waker-trigger`）无关紧要：current
    指向的基线是意图状态，不要求与实际清单逐键一致，由后续正常启动再对齐。
- `rollbackRetry=false`（首次启动 / 用户主动重启）保持原逻辑：
  差集非空 → 删新条目 + 建新快照 + current 前移（正常启动才允许改链）。

### 调用点

- 失败回滚重试：`spawnChild(pendingWake, true)`（exit 处理器 failed 分支）。
- 用户主动重启（restarting 分支）：`spawnChild(pendingWake)`（正常建快照）。
- 首次启动：`spawnChild(false)`。

### 顺序确认

回滚分支顺序已正确：**先 `current = history[prevIdx].at`（移动当前），
再 `rollbackPlugins`（逆操作）**，最后 `spawnChild(pendingWake, true)`（重试
不建快照）。

## 验证要点

- 触发失败 → 回滚 → 重试成功：current 停留在回退到的基线快照，**history 不新增**。
- 连续同因失败 → 同因保护停止（v0.4.4），不再循环卸载/装回。
- 正常启动（用户装/卸插件）→ 仍正常建新快照。
