# dsh-graceful-restart v0.4.7 — 快照存储统一为纯 diff（git commit 风格）

## 问题背景（用户反馈）

用户指出快照设计应**学习 git**：

1. **存储只写差集**：磁盘上所有条目统一只存相邻 diff，不存完整清单——
   完整基线在使用时（读取时）从链上临时组合（applyDiff 链式重建）。
2. 之前 `writeSnapshot` 让 history[0] 特例存完整 deps，格式不统一、有歧义。

## 本次改动

### writeSnapshot：所有条目统一只存 diff

- 每条 `{ diff, at, note }`——diff = 该版本相对更早版本（history[i+1]）的变化；
- 最老条目相对空清单（第一次变化 = 全部 added）；
- 无变化条目跳过（不留冗余快照）；
- **完整 deps 不再落盘**——读取时从最老到最新链式组合（applyDiff）临时重建。

### readSnapshot：链式组合（已有逻辑，天然兼容）

- `for (let i = entries.length - 1; i >= 0; i--)` 从最老到最新，
  `applyDiff(prev, h.diff)` 逐条重建完整 deps；
- `typeof h.deps === 'object'` 分支保留，兼容旧格式（迁移无缝）。

### 语义对照 git

| git | dsh-graceful-restart |
|---|---|
| commit 存相对父的变化 | 快照条目存相对更早版本的 diff |
| checkout 从对象库恢复完整树 | 读取时 applyDiff 链式组合完整清单 |
| 历史不可变、HEAD 指针 | history 不可变、current 指针 |
| 回滚 = 移动 HEAD | 回滚 = 移动 current（不修改链） |

## 迁移

当前快照已迁移为纯 diff 格式（单条目：3 包相对空 = 全部 added）。
往返验证：写（diff）→ 读（组合）→ 完整清单一致；多条目链式重建验证通过。

## 验证要点

- 正常启动建新快照：磁盘条目均为 `{diff, at}`，无完整 deps 字段。
- 读取时 history 每条都有重建的完整 deps（currentDeps / 回滚 target 正常）。
- 回滚过程链不修改（v0.4.5/6 语义保持）。
