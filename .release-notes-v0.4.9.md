# dsh-graceful-restart v0.4.9 — client bundle 精确静态检查（使用即检查）

## 问题背景（失败场景测试发现）

用坏插件 dsh-qq-client（client 半 `exports is not defined`）测试失败检测时发现：

1. **boot 输出扫描抓不到**：`exports is not defined` 只在**浏览器执行** client.js
   时抛出；dsh 启动**不执行** client.js（host 半正常 → `apply: complete`），
   stdout/stderr 无任何失败输出 → 系统误判成功、唤醒照常。
2. **HTTP 200 探测抓不到**：文件存在即 200，执行错误看不见。
3. **浏览器端无法上报**：任一 client entry 失败 → 整棵 boot 停在
   "Failed to load plugins" 错误页（fail loud, no partial UI）——包括
   graceful-restart 自己的 client 半也无法运行。

唯一能提前识别的层是**静态检查**。v0.4.3 做过但误判（要求必须定义
module/exports，把 waker-trigger 的合法 `return { inject, apply }` 写法
也判失败）。

## 本次改动（lib/index.js）

### checkClientBundleSource：使用即检查（精确）

DSH client bundle 两种合法导出写法均支持：
- A. CommonJS 风格：`var module={exports:{}}; var exports=module.exports; exports.x=...`
- B. 直接返回对象：`return { inject, apply }`（不经过 exports/module.exports）

检查原则：**只对真正使用 `exports.` / `module.exports` 的 bundle 要求对应定义**：
- 用了 `exports.` 但无 `var/const/let exports =` → 报错
- 用了 `module.exports` 但无 `module = { exports: }` → 报错
- B 风格不使用两者 → 不误判

### probeClientBundle：HTTP 200 + 静态检查两层

探测结果验证（零误判）：
- graceful-restart client.js → OK
- waker-trigger client.js（return 风格）→ OK
- dsh-qq-client client.js（exports 无定义）→ 抓到

## 验证要点

- 正常 3 包启动：刷新自检 client 检查全通过，无误判。
- 装 dsh-qq-client → 重启 → 刷新自检 client 检查抓到
  （`client.js 使用了 exports 但未定义`）→ refresh-failed → 回滚卸载 → 重试成功。
