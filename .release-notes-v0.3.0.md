# v0.3.0 — 启动守护重构为纯指针模型

## 🎯 核心变更

### 快照系统重构（纯指针模型）
- **唯一指针 `current`**（时间戳键值）指向按时间排序的快照链中当前生效版本
- **启动扫描**：实际插件清单 vs `current` 指向快照求差集
  - 差集为空 → 不变化
  - 差集非空 → 先**删除比 current 新的条目**（回滚遗留），再**新建快照**，指针前移
- **正常启动** → 不再操作快照（只清 stderr log、追加日志）

### 错误独立记录
- 启动失败不再记在快照条目上，独立 **`errors` 数组**（只保留最新一条；历史错误完整保留在 `dsh-graceful-restart.log`）
- 视图错误区改为**黑底终端风格**：完整 stderr（滚动条）+ **复制按钮**

### 无限逐级回滚（移除回滚上限）
- 失败时 `current` 向**更早移一条**（不修改快照链）→ 对差集做**完整逆操作** → 重试
- 逆操作双向处理：**新增卸载 / 被删装回 / 版本变化恢复基线版本**（`pkg@spec` 格式）
- 循环直到成功或**没有更早快照**

### 独立 stderr 捕获
- 每次启动尝试独立文件 `dsh-graceful-restart-stderr-<时间戳>.log`
- 失败时**全量读取**（不截断、不累积多次失败内容），随错误记录移交第二代展示与唤醒

## 🐛 修复
- stdio[2] 误写 `inherit` 导致 `child.stderr` 为 null（spawn 后注册中断）→ 改 `pipe`
- 入列 dup 误判（全历史 vs HEAD）与键序敏感比较 → `[当前]` 与 `⚠` 同行
- 快照链重复状态 → "无变化"条目与回滚白跳

## 📦 升级
```powershell
dsh plugin --profile web remove dsh-graceful-restart
dsh plugin --profile web add dsh-graceful-restart
```
然后完整重启（`dsh web`）。旧格式快照自动迁移（条目内 error → errors 数组，current 对象 → 时间戳键值）。

## ✅ 实测验证
- 正常启动：差集空不变化 / 装包差集非空建新快照
- 装坏包 → 自动回滚 → 恢复（唤醒消息带完整错误）
- 双插件同时装 → 失败后一起卸载
- 卸边栏+装坏包 → 逆操作同时卸载坏包 + **装回边栏**（removed→add）
- stderr 单份完整堆栈（多次失败不合并）
