# dsh-graceful-restart v0.4.0 — 两阶段统一故障判断

## 问题背景

之前的守护只判断"启动阶段"：第二代退出码非 0 或宽限期 20s 内退出 → 回滚。但
**刷新阶段没有判断**——第二代进程活着、退出码 0，并不代表浏览器刷新后能看到正常
界面。典型场景：会话恢复失败（marker 会话未恢复为 live agent）时系统误判成功，
用户界面被错误页阻挡却无人处理，直到手动干预。

## 本次改动

### 刷新阶段故障判断（第二代自检 + IPC 上报）

第二代（正式实例）启动后做刷新自检，完全复刻浏览器"连接就绪"判定：

1. **连接就绪探测**：`POST /api/host.describe` 返回 ok + `/api/events.mux`、
   `/api/events.host` 两条 downlink WebSocket 均能建立——这就是浏览器判定
   "界面正常 / 显示错误页"的同一组条件（错误页 = 该判定失败）。
2. **会话恢复**（仅唤醒场景）：marker 目标会话恢复为 live agent 并成功 steer；
   60s 超时未恢复视为刷新阶段失败。

自检结果通过 IPC 上报第一代：

- `refresh-ok`：连接就绪 +（唤醒场景）会话恢复全部通过 → 参与第一代成功判定。
- `refresh-failed`：任一失败 → 第一代走**统一故障链**（与启动失败完全同一套）：
  记 errors（独立错误记录）→ current 指针向更早移一条 → 差集逆操作
  （卸载新增 / 版本回滚 / 装回缺失）→ 重试。

### 成功判定升级为两阶段

`markSuccess` 现在要求 **宽限期存活（启动阶段）AND refresh-ok（刷新阶段）**
两个条件都满足才算成功。刷新阶段失败不再被误判为成功。

### 唤醒闸门升级

唤醒（steer）原来只等 client 探测（probe-ok）；现在还要等刷新自检通过——
**刷新失败先不唤醒**：连接就绪探测失败或会话恢复超时 → 不上报 refresh-ok、
不唤醒，直接走故障链回滚重试。15s 兜底保留（第一代探测逻辑故障时不阻塞），
但兜底同样以刷新自检通过为前提。

### 非唤醒启动同样受益

普通启动（无 marker）也做连接就绪自检：`refresh-ok` 缺失时第一代不判定成功；
失败同样触发回滚。刷新阶段守护对每次启动生效，不局限于重启场景。

## 行为变化

- 重启后会话恢复失败：不再"静默成功"，而是识别为刷新阶段故障 → 回滚重试 →
  重试代再次尝试唤醒（marker 由第一代保全重建）。
- 新增插件导致 WebSocket/describe 端点不可用（错误页场景）：启动阶段
  （退出码）可能正常，现在由刷新自检兜住并回滚。
- 错误记录（errors）与回滚日志会记录"刷新阶段自检失败"的具体原因，视图可见。

## 文件

- `lib/index.js`：`probeConnectionReady()` / `probeWs()`（刷新自检）、
  `refresh-ok` / `refresh-failed` IPC、两阶段 `markSuccess`、唤醒闸门升级。
