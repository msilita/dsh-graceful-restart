# dsh-graceful-restart v0.4.11 — 故障页检测（刷新后故障页 vs 正常页的判断通道）

## 问题背景

实测确认：client 半错误（如 dsh-qq-client 的 `exports is not defined`）是
**纯浏览器端**错误——dsh 启动（host）不执行 client.js，因此：

- boot 输出扫描（stdout/stderr）抓不到；
- 刷新自检（describe + 双 WS + HTTP 200）全通过；
- 系统误判成功、唤醒照常 → 页面实际是 "Failed to load plugins" 故障页。

## 方案：client 半 DOM 检测 + 上报 host

DSH boot 失败时（任一 client entry 加载失败），AppRoot 停留在故障页
（fail loud, no partial UI），但**各 entry fiber 独立**——本插件 client 半
（entry 未失败）仍会 apply（实测：故障页场景下 ack 正常到达）。因此：

### client 半（lib/client.js）

- apply 后轮询 `document.body.innerText`：
  - 含 **"Failed to load plugins"** → 故障页 → `POST /page-failed` 上报
    （附带收集到的失败 entry 文本）
  - 不含 → 正常页，不上报
- 仅检测、无 UI（无横幅）；2s 首检 + 3s 轮询；只上报一次。

### host 半（lib/index.js）

- 新增 `POST /plugins/dsh-graceful-restart/page-failed` 端点：
  接收上报 → 与 refresh-failed 同一处理路径——IPC refresh-failed →
  统一故障链（记 errors → current 回退 → 逆操作 → 重试）。

## 验证要点

- 正常 3 包：页面无 "Failed to load plugins" 文本 → 不上报 → 正常。
- 装 dsh-qq-client → 重启 → 页面故障页 → client 半检测到 → 上报 →
  host 走回滚 → 卸载 qq-client → 重试成功 → 页面正常。
