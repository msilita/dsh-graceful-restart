# dsh-graceful-restart v1.0.0

DSH（DeepSeek Harness）优雅重启 / 关闭 / 启动守护插件——正式版。

## 能力总览

### 1. 优雅重启 / 关闭 / 唤醒

- 启动方式不变（`dsh web`）：自动套壳，第一代常驻为隐形启动器，第二代正式服务
- `restart_harness` / `/restart`：等当前轮次结束后重启；重启+唤醒自动恢复会话并继续
- `shutdown_harness` / `/shutdown`：等轮次结束后优雅关闭
- `cancel_harness_action`：撤销安排（仅发起会话可撤销）
- 唤醒 marker 只记录**最近活跃**的会话（30 分钟窗口），闲置旧会话不唤醒

### 2. 两阶段统一故障判断（核心）

**启动阶段**（第一代）：
- 退出码非 0 / 宽限期内退出 → 失败
- **boot 输出扫描**：stdout/stderr pipe 扫描真实失败输出
  （`Failed to load plugins` / `failed to import loader entry` / `plugin tree failed`）

**刷新阶段**（事件驱动，无轮询、无超时）：
- 连接就绪持续探测（describe + 双 WS）直到成功，不设次数上限
- 会话恢复持续轮询直到成功，无 60s 超时
- **client 半故障页检测**：拦截 `console.error` + `window error`/`unhandledrejection`
  + `MutationObserver` 结构性检测——捕获 loader 真实错误输出
  （`did not activate` / `loader entry` / `exports is not defined` 等），上报 host
  走统一故障链。正常页不误报。
- 唤醒闸门纯事件驱动（等 probe-ok），无 15s 兜底

### 3. 失败自动回滚（git 风格快照链）

- **纯 `+`/`-` 差集链**：磁盘只存相邻 diff（整包粒度，版本更换 = -旧 +新），
  完整清单读取时链式组合；current = HEAD 指针
- **回滚不动链**：失败 → current 指针向更早移动 → 逆操作恢复清单 → 验证
  （逆操作后实际清单必须与目标基线逐键一致，不一致继续向更早）→ 重试
- 快照链只在正常启动（用户主动变更）时更新

### 4. 页面自动刷新

- client 半检测连接恢复 → startedAt（代际 ID）对比 → 自动 reload
- 无轮询，事件驱动

## 演进说明（git 历史已合并为单一版本）

v0.4.0 ~ v0.4.13 为开发迭代（两阶段判断、client 半检测三连迭代、快照链 git 化、
移除误判路径等），最终合并为 v1.0.0 正式版。中间版本不对外发布。
