# dsh-graceful-restart v0.4.6 — 回滚验证：逆操作未达基线视作失败，继续向更早回滚

## 问题背景（用户反馈）

v0.4.5 说"包名漂移无关紧要"——这是**错误的**。如果基线快照的键是
`@my/wake-trigger` 而实际注册名是 `dsh-waker-trigger`（插件改名后）：

- 逆操作"装回 `@my/wake-trigger`"（`add file:D:/Project/weak-trigger`）→ pnpm
  按目录 package.json 的 name 注册 → 实际还是 `dsh-waker-trigger` → **键永远
  不一致，逆操作无法达成基线**。
- 此时如果仍"保持 current 不动、假装回滚成功"并 spawn 重试 → 重试必然失败
  （清单根本不在基线状态）→ 浪费一次启动 + 可能再触发同因循环。

用户指出：**包名变化 → 逆差操作失败 → 此时就视作失败，继续回滚**（向更早的
快照找能真正达到的基线）。

## 本次改动（lib/index.js，exit 处理器 failed 分支）

"回退一条 + 逆操作 + 重试"改为**循环回滚**：

```
while (还有更早快照) {
  逆操作：把实际清单恢复到该快照 deps（CLI remove/add）
  验证：逆操作后实际清单 == 目标基线（depsEqual）？
    一致 → current 移到该快照 → spawnChild(pendingWake, true) 重试 ✓
    不一致（包名漂移/CLI 失败）→ 记日志"未达基线，继续向更早"→ prevIdx++ 继续
}
全部失败 → 放弃（保持进程存活，提示手动检查）
```

要点：

- **验证是硬性的**：`after !== null && depsEqual(after, target)`——逆操作后
  实际清单必须与目标基线逐键一致才算回滚成功，否则继续向更早。
- **快照链（history 数组）全程不修改**：current 只是指针移动，链不新建不删除。
- 包名漂移的快照被跳过（永远无法作为回滚目标），直到遇到"不含该插件"的
  更早基线（如插件安装之前）才真正达到。
- 回滚次数（rollbacks）累计所有尝试（含未达基线的）。

## 验证要点

- 触发失败 → 回滚循环：若基线含改名插件 → 打印"回滚到 X 后清单与基线不一致
  （可能包名漂移），继续向更早回滚" → 直到达到真正一致的基线 → 重试成功。
- 快照链在回滚过程中不新增/不删除（current 指针移动除外）。
