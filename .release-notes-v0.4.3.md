# dsh-graceful-restart v0.4.3 — client 半失败检测 + 页面故障横幅

## 问题背景（复现验证暴露）

复现"启动失败自动回滚"时发现：`Failed to load plugins / failed to import loader
entry (dsh-qq-client): exports is not defined`——**client 半（浏览器端）加载失败**，
但系统**没有识别**：

1. **刷新自检只查连接层**（host.describe + mux/host 双 WS），不查 client 半——
   host 正常 → refresh-ok → **唤醒照常执行**（用户："失败了，似乎你也被唤醒了"）。
2. **第一代 client probe 只查 HTTP 200**——文件存在即通过，但执行时抛
   `exports is not defined`（缺 `var module = {exports:{}}; var exports = module.exports`），
   HTTP 探测抓不到。
3. **页面模式下故障不可见**——失败信息只在终端（且 "Failed to load plugins" 打
   在 stdout，第一代只 pipe 了 stderr，连日志都捕获不到）。

## 本次改动

### 1. client bundle 静态包装检查（第一代 probe + 第二代刷新自检共用）

`probeClientBundle(pkg)`：GET `/plugins/<pkg>/client.js`，除 HTTP 200 外检查：

- 含 `__ModuleLoader__.load` 模块包装；
- 定义 `module = { exports: {} }` 且 `exports = module.exports`。

缺失 → 返回"client.js 未定义 module/exports（执行时会抛 exports is not defined）"。
实测：dsh-qq-client 被正确识别，graceful-restart 通过。

### 2. 刷新自检覆盖 client 半

第二代的 `runConnectionCheck`：连接层就绪后，遍历 profile 全部有 client 半的
插件做 bundle 检查——任一失败 → `refresh-failed`（统一故障链：记 errors →
current 回退 → 逆操作 → 重试），不再误判成功。

### 3. 页面故障横幅（client 半）

页面顶部红色横幅（z-index 最高、可关闭、5s 轮询快照）：

- errors（含"刷新阶段自检失败"）→ `⚠ <detail>`
- 最近 2 分钟内开始的回滚 → `🔄 最近回滚：...`
- 最近 2 分钟内开始的唤醒 → `⏰ 最近唤醒：...`
- 刷新失败 → `🛑 刷新阶段自检失败——系统将自动回滚重试；若持续失败请查看设置页「优雅重启」的详细错误`

页面模式下故障不再只有终端可见。

### 4. client probe 失败写入 errors

第一代探测失败时除 restoreMarkerForRetry/logRollback 外，`errors` 数组也记录
（页面横幅数据源），并 persist。

## 验证要点

- 静态检查：`node -e` 对 client.js 的包装检查（gr 通过 / qq 识别失败）。
- 完整重启后：profile 3 包正常启动 + 刷新自检含 client 检查通过。
- 安装坏 client 半插件（如 dsh-qq-client）→ 重启 → 应 refresh-failed/回滚 + 页面横幅。
