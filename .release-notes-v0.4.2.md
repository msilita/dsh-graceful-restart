# dsh-graceful-restart v0.4.2 — 唤醒范围收紧 + cancel 发起者校验

## 问题背景（完整重启测试暴露）

重启 marker 用 `ctx.agents.roots()` 记录**所有**活跃会话，导致：

1. **闲置会话被唤醒**：一个 15:52 创建、只测了"用一句话介绍你自己"的旧会话
   （session-80c3d7f3）也被记录进 marker，重启后收到继续提示开始独立跑 LLM——
   它一直 running，阻塞关闭/重启的等待（用户看不到任何输出，以为关闭没生效）。
2. **并行会话撤销关闭**：那个被唤醒的会话从日志看到"shutdown 在等我的轮次结束"，
   推理"不撤销的话系统就会关闭"，于是主动调用 `cancel_harness_action`——
   **撤销了本会话发起的关闭安排**（`cancel: pending action (shutdown) cancelled`）。

## 本次改动（lib/index.js）

### 1. 唤醒范围收紧：只唤醒最近活跃的会话

- 插件监听 `agent/status`（running/idle 转换）+ `session/event`（任何事件流）
  追踪每个会话的最近活动时间（`sessionActivity: Map<sessionId, timestamp>`）。
- `restart-wake` 写 marker 时用 `activeRootIds(ctx)`：只包含 **30 分钟内活跃过**
  的根会话；发起者（触发重启的会话）必定补入。
- 闲置的旧测试会话不再被唤醒 → 不再阻塞关闭、不再消耗算力。

### 2. cancel 发起者校验

- `scheduleExit(ctx, action, ownerId)` 记录发起者会话 id
  （工具 execute 的 `exec.agent.id`）。
- `cancelPendingExit(callerId)`：非发起者调用 → 拒绝并记录
  `cancel DENIED: caller X != owner Y`，工具返回"该安排由另一会话发起，
  仅发起会话可撤销"。
- 非 agent 调用（ownerId 为 undefined）视为发起者本人，保持向后兼容。

### 3. 等待进度增强

- 关闭/重启等待时，终端每 5s 打印在等谁，并标注**超过 60s 无事件流**的会话
  （疑似卡住），便于用户判断是否需要干预。

### 4. 绝不超时强制关闭（保持 v0.4.1 语义）

- 显式等待所有轮次结束，不打断正在运行的工作；70s 兜底重置保留
  （打印提示，可再次触发）。

## 验证要点

- 重启+唤醒：marker 只含最近活跃会话（日志 `resume marker written: <ids>`）。
- 并行会话调用 cancel_harness_action：被拒绝（日志 `cancel DENIED`）。
- 关闭等待：终端持续打印进度与疑似卡住会话。
