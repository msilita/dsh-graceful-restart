# dsh-graceful-restart v0.4.4 — boot 输出检测 + 同因防循环

## 问题背景（用户反馈）

1. **正常启动误判失败**：v0.4.3 的 client bundle 静态包装检查把 waker-trigger
   的合法写法（`return { inject, apply }`，不经过 exports/module.exports）误判
   为"未定义 exports"→ refresh-failed → 回滚。用户指出：**不要靠静态猜测，
   直接检测那个错误页面（真实错误输出）**——静态检查永远覆盖不全（写法、压缩、
   混淆无穷多）。

2. **回滚反复安装卸载**：误判触发回滚后，逆操作按历史快照里的旧包名
   `@my/wake-trigger` 装回（pnpm 实际注册为 `dsh-waker-trigger`），重试时
   差集又非空 → 又建新快照 → 又失败 → 又回滚……无限循环卸载/装回。

## 本次改动（lib/index.js）

### 1. 检测真实错误输出（取代静态猜测）

- 第二代 spawn 时 **stdout 也改为 pipe**（原先是 inherit）：转发回用户终端
  （输出不变），同时扫描启动阶段的 boot 失败特征。
- `BOOT_FAILURE_PATTERNS`：`Failed to load plugins` / `failed to import loader
  entry` / `plugin tree failed` / `failed to apply loader entry`——这些是 dsh
  启动早期打印的**真实错误信号**（"错误页面"的底层输出），client/插件加载失败
  必现其一。检测错误本身，不猜写法。
- 命中 → 记 pendingFailureDetail（含命中片段）→ kill 第二代 → exit 处理器走
  统一故障链（记 errors → current 回退 → 逆操作 → 重试）。
- 仅在启动阶段（宽限期结束前）扫描，运行期正常输出不误报；同一次 spawn 只触发一次。

### 2. 同因不再重试（防回滚循环）

- 记录上一次失败原因（failDetail）；连续两次失败原因相同 → **停止自动回滚**，
  保持进程运行并打印提示（"检测到与上次相同的失败原因，停止自动回滚（避免反复
  卸载/装回循环）。进程保持运行，请手动检查"）。
- 真实退出码失败每次 code 不同，不受影响；只有"回滚后原因依旧"（环境/探测问题）
  才触发保护。

### 3. client bundle 探测回归 HTTP 可达性

- `probeClientBundle` 只查 HTTP 200（bundle 文件存在）；加载失败由 boot 输出
  检测兜底。

## 验证要点

- 正常启动（3 包）不再误判：waker-trigger 合法写法通过，无 refresh-failed。
- 装坏 client 半插件（如 dsh-qq-client）→ 重启 → 第一代 stdout 扫描捕获
  "Failed to load plugins" → 回滚卸载 → 重试成功；若再次失败且原因相同 →
  停止回滚（不再循环）。
