# dsh-graceful-restart v0.4.10 — 移除静态检查 / 红色横幅 / 同因防循环

## 用户反馈

1. **不要静态检查**：不规定其他插件怎么写 client bundle（v0.4.9 的
   checkClientBundleSource 撤销）。
2. **不要红色横幅**：client 半的页面红色故障横幅撤销（页面保持无感）。
3. **同因防循环不行**：v0.4.4 加的"连续相同失败原因 → 停止自动回滚"会阻止
   真实的故障处理。防循环已由回滚验证承担（v0.4.6：逆操作后实际清单必须与
   目标基线一致，不达基线就继续向更早快照，不会在同一快照反复卸载装回）。

## 本次改动

- `probeClientBundle` 回归纯 HTTP 可达性（不检查内容写法）。
- client.js 移除页面横幅（banner 相关代码全部删除）。
- exit 处理器移除同因防循环（lastFailDetail 相关代码删除）；
  回滚循环由逆操作验证防循环。

## 当前能力

- 启动阶段：退出码 / 宽限期 / boot 输出扫描（Failed to load plugins 等真实信号）
- 刷新阶段：连接就绪（describe + 双 WS）+ 会话恢复
- 回滚：循环逐级 + 逆操作验证（不达基线继续向更早）+ 回滚不动快照链
- 快照：git 风格纯 +/- 差集链 + current 指针
