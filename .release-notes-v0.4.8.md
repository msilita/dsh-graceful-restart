# dsh-graceful-restart v0.4.8 — 差集只有 +/-（整包粒度，无 changed）

## 问题背景（用户反馈）

插件是**整包粒度**：任何变化都是"删旧包 + 加新包"。版本更换（版本号/地址
变化）就是 `- 旧版本 + 新版本`，**不需要 `changed` 类别**。

之前 `diffDeps` 输出三种类别（added/removed/changed），版本更换走 changed——
存储、重建、回滚都要特殊处理，且与"整包"语义不符。

## 本次改动

### diffDeps：只有 added/removed

版本更换（同包名、值不同）= `removed {name, version: 旧}` + `added {name, version: 新}`
（先卸旧版、再装新版，整包处理）。返回值 `{ added, removed }`。

### applyDiff：先删后加（无 changed 分支）

removed 按名删除、added 设新值——版本更换天然正确（先 - 旧后 + 新）。

### rollbackPlugins：逆操作只有两类

- `removed`（实际有、目标没有；含版本更换的旧版）→ 卸载
- `added`（目标有、实际没有；含版本更换的新版）→ 装回目标版本

返回 `{ added: [名字], removed: [名字] }`（无 changed）。版本更换的卸载/装回
顺序：先 remove 实际版本，再 add 目标版本。

### client.js 视图

删除 changed 展示分支——版本更换由 removed（红 `- 旧`）+ added（绿 `+ 新`）
两行自然呈现，git 风格。

## 验证

- 版本更换 `a: 1.0 → 2.0`：diff = `-a@1.0 +a@2.0` ✓
- 链式重建往返：applyDiff(v1, diff) = v2 ✓
- 回滚逆操作（实际 2.0 → 基线 1.0）：卸 a@2.0、装 a@1.0 → 等于基线 ✓
- 快照链（磁盘最新在前，读取从最老组合）：t1 → t2 组合出最新 ✓
